reviews:
  namespace: student18-bookinfo-dev
  replicas: 1
  imagePullSecrets: registry-bookinfo
  port: 8080
  image: ghcr.io/jalaler/bookinfo-reviews
  tag: dev
ingress:
  host: sitkmutt.bookinfo.dev.opsta.net
  path: "/student18/reviews(/|$)(.*)"
  serviceType: ClusterIP
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
extraEnv:
  SERVICE_VERSION: v2
  MONGO_DB_URL: mongodb://bookinfo-dev-reviews-mongodb:27017/reviews
  MONGO_DB_USERNAME: reviews
  COMMIT SHA: CHANGEME
extraEnvSecret:
  MONGO_DB_PASSWORD:
    bookinfo-dev-reviews-mongodb-secret: mongodb-password